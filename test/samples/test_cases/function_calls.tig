let
    var a : int := 1
    var b : int := 10

    function func (step : int) : int = 
        let var sum := 0 in
            for i := a to b do
                sum := sum + step;
            sum
        end
in
    printint(func(3));
	print("\n")
end
